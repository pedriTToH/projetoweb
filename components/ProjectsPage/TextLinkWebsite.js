import styled from 'styled-components'

const TextLinkWebsite = styled.a`
    color: ${props => props.Color};
    font-family: 'Signika', sans-serif;
    font-weight: 400;
    font-size: 1.4rem;
    text-decoration: none;
`

export default TextLinkWebsite;